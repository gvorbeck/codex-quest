import { gnome } from "./gnome";
import { GNOME_NAME } from "./names";

export const races = { gnome };

export enum RaceNamesTwo {
  DWARF = "Dwarf",
  ELF = "Elf",
  GNOME = GNOME_NAME,
  HALFLING = "Halfling",
  HUMAN = "Human",
  CUSTOM = "Custom",
}
